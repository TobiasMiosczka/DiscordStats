package com.github.tobiasmiosczka.discordstats.rest;

//@RunWith(SpringRunner.class)
//@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
public class IntegrationTest {

    //@Autowired
    //private TestRestTemplate restTemplate;

    /*@Test
    public void returnGuild() throws  Exception {
        //act
        ResponseEntity<DiscordGuild> response = restTemplate.getForEntity("/api/guild/0", DiscordGuild.class);
        //assert
        assertThat(response.getStatusCode()).isEqualTo(HttpStatus.OK);
        assertThat(response.getBody().getId()).isEqualTo(0);
        assertThat(response.getBody().getName()).isEqualTo("TestGuild");
        assertThat(response.getBody().getIconUrl()).isEqualTo("TestUrl");
    }*/

}

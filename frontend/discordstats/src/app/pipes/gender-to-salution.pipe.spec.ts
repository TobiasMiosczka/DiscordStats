import { GenderToSalutionPipe } from './gender-to-salution.pipe';

describe('GenderToSalutionPipe', () => {
  it('create an instance', () => {
    const pipe = new GenderToSalutionPipe();
    expect(pipe).toBeTruthy();
  });
});
